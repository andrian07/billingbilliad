name: Release Windows

on:
  push:
    branches: [main]
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.44.6"
          channel: "stable"

      - run: flutter config --enable-windows-desktop

      - run: flutter pub get

      - run: flutter build windows --release

      # flutter build windows menghasilkan folder (exe + dll + data/) di bawah ini - dizip supaya
      # bisa diunduh sebagai satu file dari halaman Actions/Releases
      - name: Zip build output
        shell: pwsh
        run: |
          Compress-Archive -Path build/windows/x64/runner/Release/* -DestinationPath billingbilliad-windows.zip

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: billingbilliad-windows
          path: billingbilliad-windows.zip

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: billingbilliad-windows.zip
          generate_release_notes: true
